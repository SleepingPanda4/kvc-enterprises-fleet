import type { Metadata } from "next"; import { eq } from "drizzle-orm"; import { getDb } from "../../../db"; import { vehicles } from "../../../db/schema"; import { VehicleDetail } from "./VehicleDetail";
async function record(id:string){try{const[row]=await getDb().select().from(vehicles).where(eq(vehicles.id,Number(id))).limit(1);return row}catch{return undefined}}
export async function generateMetadata({params}:{params:Promise<{id:string}>}):Promise<Metadata>{const{id}=await params,v=await record(id);const title=v?`Vehicle #${v.number} | KVC Enterprises`:"Vehicle | KVC Enterprises",description=v?`${v.year?`${v.year} `:""}${v.makeModel}, ${v.routeNumber||"unassigned"}. Issue history and fleet status.`:"KVC Enterprises vehicle details.";return{title,description,openGraph:{title,description,images:[]},twitter:{title,description,images:[]}}}
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <VehicleDetail id={id}/>}
