import { getDb } from "../../../db"; import { issues } from "../../../db/schema";
export async function POST(req:Request){try{const p=await req.json() as Record<string,string>;const vehicleId=Number(p.vehicleId),notes=p.notes?.trim(),type=p.type==="Other"?p.customType?.trim():p.type;if(!vehicleId||!notes||!type)return Response.json({error:"Vehicle, type, and notes are required"},{status:400});const db=getDb();const[row]=await db.insert(issues).values({vehicleId,type,notes}).returning();return Response.json({issue:row},{status:201})}catch(e){return Response.json({error:e instanceof Error?e.message:"Could not report issue"},{status:500})}}
