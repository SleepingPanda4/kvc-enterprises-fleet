import { and,eq,ne } from "drizzle-orm"; import { getDb } from "../../../../../db"; import { vehicles } from "../../../../../db/schema";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params,targetId=Number(id),p=await req.json() as {routeNumber?:string|null;confirmReassign?:boolean},routeNumber=p.routeNumber?.trim()||null,db=getDb(),now=new Date().toISOString();const[target]=await db.select({id:vehicles.id}).from(vehicles).where(eq(vehicles.id,targetId)).limit(1);if(!target)return Response.json({error:"Vehicle not found"},{status:404});let owner:typeof vehicles.$inferSelect|undefined;if(routeNumber)[owner]=await db.select().from(vehicles).where(and(eq(vehicles.routeNumber,routeNumber),ne(vehicles.id,targetId))).limit(1);if(owner&&!p.confirmReassign)return Response.json({error:`Route ${routeNumber} is already assigned to vehicle #${owner.number}.`,conflict:{vehicleId:owner.id,vehicleNumber:owner.number,routeNumber}},{status:409});if(owner){await db.batch([db.update(vehicles).set({routeNumber:null,updatedAt:now}).where(eq(vehicles.id,owner.id)),db.update(vehicles).set({routeNumber,updatedAt:now}).where(eq(vehicles.id,targetId))])}else await db.update(vehicles).set({routeNumber,updatedAt:now}).where(eq(vehicles.id,targetId));const[row]=await db.select().from(vehicles).where(eq(vehicles.id,targetId)).limit(1);return Response.json({vehicle:row,unassignedVehicleId:owner?.id||null})}catch(e){return Response.json({error:e instanceof Error?e.message:"Route assignment could not be saved."},{status:500})}}
