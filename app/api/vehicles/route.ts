import { eq, sql } from "drizzle-orm"; import { getDb } from "../../../db"; import { issues,vehicleModels,vehicles } from "../../../db/schema";
export async function GET(){try{const db=getDb();const rows=await db.select({id:vehicles.id,number:vehicles.number,routeNumber:vehicles.routeNumber,makeModel:vehicles.makeModel,year:vehicles.year,openIssues:sql<number>`sum(case when ${issues.status} = 'open' then 1 else 0 end)`}).from(vehicles).leftJoin(issues,eq(vehicles.id,issues.vehicleId)).groupBy(vehicles.id).orderBy(vehicles.number);return Response.json({vehicles:rows})}catch(e){return Response.json({error:e instanceof Error?e.message:"Could not load vehicles"},{status:500})}}
export async function POST(req:Request){try{const p=await req.json() as Record<string,string>,routeNumber=p.routeNumber?.trim()||null;const makeModel=(p.makeModel||p.presetModel||"").trim();if(!p.number?.trim()||!makeModel)return Response.json({error:"Vehicle number and model are required"},{status:400});const db=getDb();if(routeNumber){const[owner]=await db.select({number:vehicles.number}).from(vehicles).where(eq(vehicles.routeNumber,routeNumber)).limit(1);if(owner)return Response.json({error:`Route ${routeNumber} is already assigned to vehicle #${owner.number}.`},{status:409})}await db.insert(vehicleModels).values({name:makeModel}).onConflictDoNothing();const[row]=await db.insert(vehicles).values({number:p.number.trim(),routeNumber,makeModel,year:p.year?Number(p.year):null}).returning();return Response.json({vehicle:row},{status:201})}catch(e){const message=e instanceof Error?e.message:"Could not add vehicle";return Response.json({error:message.includes("vehicles_route_number_unique")?"That route is already assigned.":message},{status:message.includes("vehicles_route_number_unique")?409:500})}}
